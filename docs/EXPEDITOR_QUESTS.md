Вопросы для технарей, занимающихся вашим приложением:

1. Нужен Swagger/OpenAPI документация по серверной стороне вашего web-приложения. Ссылкой или файлом.

2. Как идентифицируется водитель в системе?
driver_id
телефон
ФИО + колонна
другой IDчто бы понять, по какому ключу его связывать с водителем в Боте.

3. Есть ли у нарушения / тикета уникальный ID (ticket_id, violation_id и т.п.),
который можно использовать при обмене данными между системами.
чтобы после прохождения теста отправить результат именно по этому нарушению.

4. Как сейчас определяется область нарушения водителя? Я так понял что ее указывают логисты/механы 
есть ли тип / код нарушения
есть ли категория
или это просто текстовое описаниеИ возможно ли передавать нам код темы теста, который водитель должен пройти.

5. Какой способ взаимодействия backend предполагается? самый оптимальный - ваш backend вызывает наш endpoint, например:
POST /api/violations
И передает информацию о нарушении.

6. Минимальные данные для интеграции - для работы желательно, чтобы ваш backend мог передавать:
violation_id
driver_id
ФИО
телефон
номер колонны
тип / код нарушения
тема теста (если есть)
комментарий
дата создания
дедлайнесли каких то полей нет - важно понимать какие доступны

7. Что ваш бэк будет получать от бота, например бот может отправлять обратно:
violation_id
driver_id
test_topic
result (passed / failed)
score
attempt
timestampТо есть - надо понять, какие данные вам необходимы, что бы автоматически закрывать нарушение.

ТАК ТО: первый пункт если решить - он (вероятно) решит почти все остальные вопросы